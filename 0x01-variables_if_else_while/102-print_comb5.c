#include <stdio.h>
/* more headers goes there */

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int zero = 0;
	int one = 1;
	int zeroc = 48;
	int onec = 49;
while (zero < 10) {
while (one < 10)
{
putchar(zeroc);
putchar(zeroc);
putchar(' ');
putchar(zeroc);
putchar(onec);
putchar(',');
putchar(' ');

}
one = zero;
onec = zeroc;
}

}
