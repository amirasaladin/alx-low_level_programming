#include "main.h"
#include <string.h>
/**
 * _strcpy - Check code
 * @src: char
 * @dest: char
 * Return: the return value data
*/

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
