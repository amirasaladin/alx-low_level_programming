#include "main.h"
#include <string.h>
/**
 * _strcat - Check code
 * @dest: string
 * @src: string
 * Return: the return value data
*/

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
