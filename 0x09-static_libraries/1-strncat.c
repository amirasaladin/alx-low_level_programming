#include "main.h"
#include <string.h>
/**
 * _strncat - Check code
 * @dest: string
 * @src: string
 * @n: size_t
 * Return: the return value data
*/

char *_strncat(char *dest, char *src, int n)
{
	return (strncat(dest, src, n));
}
