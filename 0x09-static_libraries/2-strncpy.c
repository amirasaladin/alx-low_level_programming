#include "main.h"
#include <string.h>
/**
 * _strncpy - Check code
 * @dest: string
 * @src: string
 * @n: size_t
 * Return: the return value data
*/

char *_strncpy(char *dest, char *src, int n)
{
	return (strncpy(dest, src, n));
}
